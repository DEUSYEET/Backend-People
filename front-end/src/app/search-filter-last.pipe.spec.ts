import { SearchFilterLastPipe } from './search-filter-last.pipe';

describe('SearchFilterLastPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterLastPipe();
    expect(pipe).toBeTruthy();
  });
});
